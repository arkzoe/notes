- ofstream
	- cerr无缓冲
	- fmtflags int
		- dec：整数I/O使用十进制，std::dec
		- oct：整数I/O使用八进制，std::oct
		- hex：整数I/O使用十六进制，std::hex
		- basefiled：dec|oct|hex，适用于掩码运算
		- left：左矫正，添加填充字符到右，std::left
		- right：右矫正，添加填充字符到左，std::right
		- ...
	- cout有缓冲
	- 控制台输出重定向命令
- cin格式化输出错误处理
	- cin.rdstate()
		- goodbit：无错误0
		- badbit：不可恢复的流错误1
		- failbit：输出/出入操作失败（格式化/提示错误）2
		- eofbit：关联的输出序列抵达文件尾
		- good()
		- fail()
		- eof()
		- bad()
- stringstream格式化输入输出
- fstream
	- ifstream
	- ofstream
- 文件打开模式
	- app：写入前寻位到流结尾
	- binary：二进制打开
	- in：为读打开
	- out：为写打开
	- trunc：打开时舍弃流的内容，清空
	- ate：打开后立即寻位到流的结尾
- 文件状态：iostate
	- goodbit：无错误
	- badbit：不可恢复的流错误
	- failbit：输入/输出操作失败
	- eofbit：关联的输出序列已抵达文件尾
- 移动文件读写位置：seekdir
	- beg：流的开始
	- end：流的结尾
	- cur：流位置指示器的当前位置
	- seekg(off_type off, std:ios_base :: seekdir dir)--pos_type tellg()
	- seekp(off_type off, std :: ios_base :: seekdir dir)--pos_type tellp()
- 读写文件
- 将输入缓冲区与关联数据源同步
	- sync
	- flush