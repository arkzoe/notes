- if
- 条件转换成bool表达式
- bool存放俩个值
	- sizeof(bool)的值实现定义，可不为1
	- 算术
		- 位与，位或，位非
	- 逻辑
		- 代用运算符not,or,and
	- 比较
bitset输出二进制
数字转字符sto类型
枚举
- 消息类型
- 日志
枚举类
- enum class xxx{}
- 访问必须加类型名
- main
	- argc：参数个数
	- argv：字符串数组
		- 第一个参数执行程序完整路径
		- 2：用户传递