- 开闭原则（OCP）
	- 拓展开放修改关闭
- 单一职责原则（SRP）
	- 一个类只做一件事
- 里氏替换原则（LSP）
	- 引用基类的地方必须能透明的使用子类对象
- 依赖倒置原则（DIP）
	- 高层不依赖底层，只依赖抽象
	- 抽象不依赖细节
	- 细节因该依赖抽象
- 接口隔离原则（ISP）
	- 不依赖不需要的接口
	- 设计接口类
	- 有可能在单一职责的基础上，再分接口
- 迪米特法则（最少知道原则/LoD）
	- 一个类对其他类尽量减少了解
- 合成/聚合复用原则（CARP）
	- 优先通过包含(组合/聚合)其他对象来复用功能
	- 降低耦合