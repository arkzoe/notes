- 注释
	- 多行三引号
- 变量及类型
	- 变量名 = 变量值
	- int
	- float
	- bool
	- string：多行字符串三引号
- 标识符和关键字
- 输出
	- print默认换行
	- end=‘\n’
	- 格式化输出占位符
	- %s
	- %d
	- %f
	- %5d：期望五位数，不够补空格/0
	- %.2f：保留两位小数，四舍五入
- 输入
	- input
- 运算符
- 类型转化
	- int(x)
	- float(x)
	- str(x)
	- bool()
	- eval()：去掉引号，是啥就是啥，转字符串没有这个变量就报错
- 流程控制
	- 顺序
		- if...elif...else
		- 三元
	- 分支
	- 循环
		- while 条件:
		- for 临时变量 in 容器类型--列表或者字符串:
			- range(起始值，结束值，步长)
				- 起始值默认0，步长默认1
		- 循环+else
			- 不是break就会走