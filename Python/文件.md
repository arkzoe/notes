```python
f = open(name,mode,encoding=)
# 默认gbk
# name:文件名/路径
#		//，r''
# mode:设置打开模式
#	read,write,append

f.read() #读所有
f.readline() #读一行
f.write()
f.close()

with open(name,mode,encoding=) as 别名,...
# with后变量会自动释放
```
![[mode模式.png]]